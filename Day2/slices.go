package main

import "fmt"

func main() {
	fmt.Println("Hello, Yash!")
	p := []int{1, 2, 3, 4, 5, 6, 7}
	fmt.Println(p)
}

// slice is more used than arrays since it is dynamic
// dont have to declare size and can just change the values
// it is not fixed size or length but need to protect the type
// as in it must be of declared type
